---
deployment:
  tasks:
    - rsync -a --exclude='vendor' --exclude='.git' --exclude='.env' --exclude='storage' --exclude='bootstrap/cache' $SRCDIR/ $HOME/laravel/
    - chmod -R 755 $HOME/laravel/storage $HOME/laravel/bootstrap/cache
